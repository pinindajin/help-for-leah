const Dogly = require('./dogly'); // Import the Dogly library
const dogly = new Dogly();        // Create new instance of Dogly

/*
    Dogly is a library that get's you dog images by breed as well as
    can list the name of different breeds.

    It's api consists of the following methods

    getBreeds(function callback(breeds) {})
    getBreedImages(string breedName, function callback(images) {})
    getBreedrandomImage(string breedName, function callback(image) {})

    Practice below using the library. 
*/

